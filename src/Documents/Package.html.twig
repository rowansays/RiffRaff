{% extends 'base.html.twig' %}

{% block content %}
    {% include 'components/breadcrumbs.html.twig' %}

    <article class="Element -package">
        <h2 class="DocumentTitle">{{ node.name == '\\' ? 'API Documentation' : node.name }}</h2>
        {{ include('components/Toc.html.twig', {'packages': node.children}) }}
        {{ include('components/constants.html.twig') }}
        {{ include('components/functions.html.twig') }}
    </article>
{% endblock %}
